(function () { // Определяем анонимную функцию
    // Отсутствие имени означсает отсутствие глобального имени

    // Тело функции
    // здесь можно объявлять переменные - это не приведет к созданию глобальных переменных.

    var message = "Module3"; // переменная не глобальная
    alert(message);
})(); // конец определения функции и ее вызов.